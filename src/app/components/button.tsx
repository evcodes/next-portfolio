import Image from "next/image";

export default function Button() {
  return (
    <main className="default-btn">
      <div>
        <button>Click me</button>
        <div>
        </div>
      </div>
    </main>
  );
}
